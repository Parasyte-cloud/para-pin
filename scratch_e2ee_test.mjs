// (unused scratch file — the E2EE crypto scheme it validated is now excluded
// from deployment via .assetsignore; kept empty rather than deleted since
// this filesystem doesn't allow removing files.)
