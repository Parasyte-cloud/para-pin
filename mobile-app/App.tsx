// Unused — this project uses expo-router (see package.json's "main":
// "expo-router/entry" and the app/ directory for routes/layouts).
// Left in place only because this sandbox can't delete files; safe to
// `rm App.tsx index.ts` locally, nothing imports them.
export default function UnusedLegacyEntry() {
  return null;
}
